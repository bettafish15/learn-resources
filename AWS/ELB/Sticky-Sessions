Sticky Sessions (Session affinity):
	- same client is always redirected to the same instance behind a load balancer
	- App Load Balancer, Network Load Balancer, Classic Load Balancer can use
	- cookie used for stickiness has an exp date you control
	- may bring imbalance to ec2 instance

cookie names:
	- app based cookies:
		+ custom cookie:
			. generated by target
			. can include any custom attributes
			. must be specified individually for each target group
			. dont use AWSALB, AWSALBAPP, AWSALBTG (reserved for use by ELB)

		+ app cookie:
			. generated by load balancer
			. name = AWSALPAPP

	- duration based:
		+ generated by load balancer
		+ name is AWSALB for ALB, AWSELB for CLB